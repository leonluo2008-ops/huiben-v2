# USER.md

## 项目设计理念

### 核心理念：标准化是第一优先级

**通用框架就像编程语言规范。写 Python 要遵循 PEP8，建 Agent 或项目也要遵循通用标准结构，才能跨平台、跨项目复用。**

---

## 1. 所有 Agent / 项目必须遵循统一结构

无论主 Agent 还是子 Agent，无论用什么语言或框架，**结构必须统一**，这样才能：
- 统一管理（Evolver 等工具能扫描）
- 跨项目复制
- 接入其他平台
- 长期维护

### 标准结构（必须全部包含）

```
项目根目录/
├── SOUL.md        # Agent 身份与使命
├── AGENTS.md      # Agent 角色定义
├── USER.md        # 用户偏好与规则（本文件）
├── MEMORY.md      # 长期记忆
├── memory/        # 运行时日志（Evolver 扫描用）
│   ├── sessions/  # Session 日志
│   └── daily/    # 每日记录
├── prompt.md      # 提示词模板
└── [业务文件]     # 具体代码/内容
```

### 为什么 memory/ 必须有

- **Evolver 扫描日志**发现错误模式，生成改进 prompt
- 没有 memory/ 的项目，Evolver 只能读混杂的历史记录
- 子 Agent 也必须有自己的 memory/，否则无法被独立进化

---

## 2. 每次创建 Agent 或项目时的强制规则

**创建前**：先确认目标路径是否已有项目，避免重复创建空壳。

**创建时必须包含**：
1. SOUL.md — Agent 身份定义
2. AGENTS.md — Agent 职责范围
3. USER.md — 用户偏好（本文件可共享）
4. MEMORY.md — 初始化空文件
5. memory/sessions/ — 运行时日志目录
6. memory/daily/ — 每日记录目录

**禁止**：
- 创建"裸项目"（只有代码，没有结构文件）
- 用临时方案替代标准结构
- 跳过任何必需文件

---

## 3. 设计原则

- **通用性优先**：方案必须能跨平台、跨项目复制，不依赖特定环境
- **约定大于配置**：遵循已有规范，不重复造轮子
- **可演进**：结构支持后续扩展（如接入 Evolver）
- **不妥协**：遇到模糊场景，优先遵循标准，而非快速凑合

---

## 4. 与其他工具的协同

### Evolver 集成
- Evolver 扫描 memory/ 目录进行日志分析
- 所有 Agent 项目都必须有 memory/ 才能被 Evolver 管理
- 主 Agent 和子 Agent 可以各自有独立的 Evolver 实例

### OpenClaw 框架
- 遵循 OpenClaw 的 workspace 结构约定
- sessions_spawn 等协议输出能被 OpenClaw 自动解析

---

## 5. 记录历史决策

当重要架构决策完成后，**必须更新 MEMORY.md**，包括：
- 新建了哪些 Agent / 项目
- 关键设计选择及其理由
- 废弃了哪些方案及原因

这有助于后续维护和知识传承。
